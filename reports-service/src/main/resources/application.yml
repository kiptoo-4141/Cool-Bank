spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cool_bank
    username: postgres
    password: Lestitans@6
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate # or 'update' for dev
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    mongodb:
      uri: mongodb://localhost:27017/report_db
      database: cool_bank